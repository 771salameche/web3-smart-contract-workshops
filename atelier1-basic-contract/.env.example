# Fichier d'exemple pour les variables d'environnement (.env.example)
# Ce fichier contient des exemples de variables d'environnement nécessaires au projet.
# NE JAMAIS COMMITER CE FICHIER AVEC DES CLÉS OU DES INFORMATIONS SENSIBLES !
# Créez une copie de ce fichier nommée .env et remplissez-la avec vos vraies valeurs.

# Example file for environment variables (.env.example)
# This file contains examples of environment variables required for the project.
# NEVER COMMIT THIS FILE WITH SENSITIVE KEYS OR INFORMATION!
# Create a copy of this file named .env and fill it with your real values.

# --- Clé privée du compte de déploiement (PRIVATE_KEY) ---
# Votre clé privée Ethereum pour le compte qui déploiera les contrats.
# GARDER CETTE CLÉ SECRÈTE ! NE LA PARTAGEZ JAMAIS !
# Comment obtenir votre clé privée depuis MetaMask :
# 1. Ouvrez MetaMask.
# 2. Sélectionnez le compte que vous souhaitez utiliser.
# 3. Cliquez sur les trois points verticaux à droite du compte, puis "Détails du compte".
# 4. Cliquez sur "Exporter la clé privée".
# 5. Entrez votre mot de passe pour révéler votre clé privée.
# 6. Copiez la clé (commençant par 0x) ici.
#
# Your Ethereum private key for the account that will deploy the contracts.
# KEEP THIS KEY SECRET! NEVER SHARE IT!
# How to get your private key from MetaMask:
# 1. Open MetaMask.
# 2. Select the account you want to use.
# 3. Click the three vertical dots to the right of the account, then "Account details".
# 4. Click "Export Private Key".
# 5. Enter your password to reveal your private key.
# 6. Copy the key (starting with 0x) here.
PRIVATE_KEY="0x_YOUR_METAMASK_PRIVATE_KEY_HERE"

# --- URL RPC pour Polygon Amoy ---
# L'URL du nœud RPC pour le réseau de test Polygon Amoy.
# Vous pouvez obtenir ceci depuis un service comme Alchemy, Infura ou QuickNode,
# ou utiliser l'URL publique de Polygon Technology.
#
# RPC URL for Polygon Amoy
# The RPC node URL for the Polygon Amoy testnet.
# You can get this from a service like Alchemy, Infura, or QuickNode,
# or use the public URL from Polygon Technology.
AMOY_RPC_URL="https://rpc-amoy.polygon.technology"

# --- Clé API Polygonscan (POLYGONSCAN_API_KEY) ---
# Clé API pour la vérification des contrats sur Polygonscan (Amoy utilise api.polygonscan.com).
# Comment obtenir votre clé API Polygonscan :
# 1. Allez sur https://polygonscan.com/myapikey
# 2. Créez un compte ou connectez-vous.
# 3. Générez une nouvelle clé API.
# 4. Copiez la clé ici.
#
# Polygonscan API Key (POLYGONSCAN_API_KEY)
# API key for contract verification on Polygonscan (Amoy uses api.polygonscan.com).
# How to get your Polygonscan API key:
# 1. Go to https://polygonscan.com/myapikey
# 2. Create an account or log in.
# 3. Generate a new API key.
# 4. Copy the key here.
POLYGONSCAN_API_KEY="YOUR_POLYGONSCAN_API_KEY_HERE"

# --- Obtenir du MATIC de test pour Amoy ---
# Vous aurez besoin de MATIC de test pour payer les transactions sur Amoy.
# Obtenez-en depuis le faucet : https://faucet.polygon.technology/
#
# Getting test MATIC for Amoy
# You will need test MATIC to pay for transactions on Amoy.
# Get it from the faucet: https://faucet.polygon.technology/

# IMPORTANT: Polygon Amoy est le nouveau réseau de test recommandé. Mumbai est maintenant déprécié.
# IMPORTANT: Polygon Amoy is the new recommended testnet. Mumbai is now deprecated.
